import 'package:flutter/material.dart';

class AppColors {
  // ── Brand / Primary ──────────────────────────────
  static const Color primary        = Color(0xFF2563EB); // xanh dương chính
  static const Color primaryFg      = Color(0xFFFFFFFF);

  // ── Backgrounds ──────────────────────────────────
  static const Color background     = Color(0xFFF0F4F9); // nền trang
  static const Color card           = Color(0xFFFFFFFF); // nền card
  static const Color inputBg        = Color(0xFFF1F5F9);
  static const Color secondary      = Color(0xFFE8EDF5);

  // ── Text ─────────────────────────────────────────
  static const Color foreground     = Color(0xFF0F172A); // text chính
  static const Color muted          = Color(0xFFE2E8F0);
  static const Color mutedFg        = Color(0xFF64748B); // text phụ / caption

  // ── Semantic ─────────────────────────────────────
  static const Color success        = Color(0xFF22C55E); // Thu / dương
  static const Color successBg      = Color(0xFFDCFCE7);
  static const Color danger         = Color(0xFFEF4444); // Chi / âm
  static const Color dangerBg       = Color(0xFFFEE2E2);
  static const Color warning        = Color(0xFFF59E0B);
  static const Color warningBg      = Color(0xFFFEF3C7);
  static const Color purple         = Color(0xFF7C3AED);
  static const Color purpleBg       = Color(0xFFEDE9FE);

  // ── Border ───────────────────────────────────────
  static const Color border         = Color(0x140F172A); // rgba(15,23,42,0.08)

  // ── Chart colors ─────────────────────────────────
  static const Color chart1         = Color(0xFF2563EB);
  static const Color chart2         = Color(0xFF7C3AED);
  static const Color chart3         = Color(0xFF059669);
  static const Color chart4         = Color(0xFFD97706);
  static const Color chart5         = Color(0xFFDB2777);
  static const Color chart6         = Color(0xFF64748B);

  // ── KPI card accent colors ────────────────────────
  static const Color kpiThu         = Color(0xFF22C55E);
  static const Color kpiChi         = Color(0xFFEF4444);
  static const Color kpiBalance     = Color(0xFF2563EB);
  static const Color kpiCount       = Color(0xFF7C3AED);
}
